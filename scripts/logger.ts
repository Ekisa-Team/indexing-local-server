const log = console.log.bind(console);
const error = console.error.bind(console);
const info = console.info.bind(console);
const warn = console.warn.bind(console);

export const Logger = { log, error, info, warn };
